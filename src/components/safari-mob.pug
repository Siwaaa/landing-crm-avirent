mixin safari-mob(imgPath, id, startActive, ...items)
  .browser-mob
    .browser-mob__content
      each item in items
        img(src=`${imgPath}${item}.png` class=`${item == startActive ? 'active': ''} ${attributes.class}` id=id+item loading="lazy")